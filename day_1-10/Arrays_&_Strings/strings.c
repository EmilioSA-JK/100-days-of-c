#include <stdio.h>

int main(){

    // A string is considered an array
    char text[] "Hi nih";
    int age = 42;

    printf(text);

    

    // The specifier for string is %s

    char name[] = "James";
    int age = 42;

    printf("%s is %d years old.", name, age);

    return 0;
}